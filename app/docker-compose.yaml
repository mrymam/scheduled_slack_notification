version: '3.8'
services:
  dev:
    container_name: dev
    build:
      context: .
      dockerfile: docker/dev.Dockerfile
    # command: make dev
    env_file:
      - .env
    environment:
      - GO_ENV
    volumes:
      - .:/go/src/app
    working_dir: /go/src/app
